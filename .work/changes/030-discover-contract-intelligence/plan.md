# Discover Contract Intelligence Plan

1. Define strict request, budget, document, operation, schema, relationship, unknown, omission, and response contracts and schemas.
2. Compose only existing bounded scanner and read-authority services.
3. Detect local OpenAPI, JSON Schema, and MCP contract candidates by path and filename conventions.
4. Parse dependency-free JSON, preserve YAML as an explicit unsupported capability, and isolate per-document failures.
5. Extract deterministic operations, schemas, and reference topology.
6. Apply caller budgets after safe configured discovery so omission counts remain truthful.
7. Run focused, full Discover, scope, whitespace, architecture, and locked repository verification.
8. Publish through an exact-head PR, merge, close governance, and remove branch/worktree state.
