# Discover Impact Graph Plan

1. Define strict request, budget, result, omission, unknown, and Work-handoff contracts and schemas.
2. Compose existing bounded scanner, Python AST index, and verification discovery services.
3. Map changed Python paths to declared symbols and conservative reverse import, call, and inheritance dependants.
4. Select affected tests using AST connections first and filename conventions second.
5. Apply caller budgets after safe configured discovery so omission counters remain truthful.
6. Prove determinism, schema validity, degradation behavior, architecture boundaries, and full repository verification.
7. Publish through an exact-head PR, merge, close governance, and remove local and remote branch/worktree state.
